if (typeof pagelet_composer != "undefined") {
    document.getElementById("pagelet_composer").remove();
}

if (typeof pagelet_timeline_composer != "undefined") {
    document.getElementById("pagelet_timeline_composer").remove();
}